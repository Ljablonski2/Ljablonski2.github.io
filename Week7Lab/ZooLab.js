function giraffe(giraffename, giraffeage, giraffetype, giraffeimage) {
  this.giraffename = giraffename;
  this.giraffeage = giraffeage;
  this.giraffetype = giraffetype;
  this.giraffeimage = giraffeimage;
}

function bear(bearname, bearage, beartype, bearimage) {
  this.bearname = bearname;
  this.bearage = bearage;
  this.beartype = beartype;
  this.bearimage = bearimage;

}

function cat(catname, catage, cattype, catimage){
  this.catname = catname;
  this.catage = catage;
  this.cattype = cattype;
  this.catimage = catimage;
}

  // var new giraffe("Joey","10","girraffe" )
  // var new bear("Sam", "2", "bear")
  // var new cat("Jo", "11", "cat")

}

var animal = ["giraffe", "bear", "cat"]

//   var animal = ["giraffe", "bear", "cat"];
generateRandomIndex(maxIndex)



//   var name = ["Sarah", "Bill", "Jim"]:

// generateRandomIndex(maxIndex)


// Math.floor(Math.random(maxIndex)